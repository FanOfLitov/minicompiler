struct Pair {
    int left;
    int right;
}

fn sum(int a, int b) -> int {
    int result = a + b;
    return result;
}

fn main() {
    for (int i = 0; i < 3; i += 1) {
        if (i < 2) {
            sum(i, i + 1);
        } else {
            while (false) {
            }
        }
    }
}
